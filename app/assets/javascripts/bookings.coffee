# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/
# $(document).on 'turbolinks:load', ->
#   $('#datepicker').datepicker
#     format: 'yyyy-mm-dd'
#     todayBtn: true
#     language: 'ru'
#     daysOfWeekHighlighted: '0,6'
#     todayHighlight: true
#     datesDisabled: disa
#     autoclose: true


    
    # return
#     $(document).ready ->
# start1 = $('#start').datepicker('getDate')
# stop1 = $('#out').datepicker('getDate')

# sum = (start1, stop1) ->
#   return (stop1 - start1) / (1000 * 60 * 60 * 24)
#   $('#TextBox3').val sum
#   return

    # $(document).ready "turbolinks:load", ->
    #   $('#datepicker').datepicker
    # format: 'yyyy-mm-dd'
    # autoclose: true
    # weekStart: 1
    # maxViewMode: 2
    # todayBtn: true
    # language: 'ru'
    # daysOfWeekHighlighted: '0,6'
    # calendarWeeks: true
    # todayHighlight: true
    # # datesDisabled: disa


  # $(document).ready ->
  # $('input[class="daterange"]').daterangepicker()
  # return